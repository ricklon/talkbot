# OpenRouter Configuration
OPENROUTER_API_KEY=your_api_key_here

# Optional: For OpenRouter rankings
OPENROUTER_SITE_URL=https://your-site.com
OPENROUTER_SITE_NAME=TalkBot

# Local-first defaults
TALKBOT_LLM_PROVIDER=local_server
TALKBOT_DEFAULT_TTS_BACKEND=kittentts
TALKBOT_DEFAULT_MODEL=qwen3-1.7b-q4_k_m
TALKBOT_DEFAULT_USE_TOOLS=1
TALKBOT_LOCAL_MODEL_PATH=./models/qwen3-1.7b-q4_k_m.gguf
TALKBOT_LOCAL_N_CTX=8192
# Optional: OpenAI-compatible local server provider (llama-server / llama_cpp.server)
TALKBOT_LOCAL_SERVER_URL=http://127.0.0.1:8000/v1
# Optional model identifier sent to the server (defaults to local model path or --model)
TALKBOT_LOCAL_SERVER_MODEL=models/default.gguf
# Optional API key for local server if enabled
# TALKBOT_LOCAL_SERVER_API_KEY=your_local_server_key
# Optional if llama-cpp-python is installed.
# Set this only when using external llama.cpp CLI:
# TALKBOT_LLAMACPP_BIN=/full/path/to/llama-cli
TALKBOT_ENABLE_THINKING=0

# OpenRouter-specific default model (use when TALKBOT_LLM_PROVIDER=openrouter)
# TALKBOT_DEFAULT_MODEL=mistralai/ministral-3b-2512

# Agent personality prompt (optional).
# When set, all CLI commands (chat, say, tool, voice-chat) use this as the
# default system prompt. The GUI Prompt tab is pre-populated from this value.
# Override per-command with --system/-s.
TALKBOT_AGENT_PROMPT="You are a voice-first assistant with access to tools. Be extremely brief.

## Rules
- If a tool fits the request, call it immediately. Do not explain or narrate.
- Questions (\"Can you...\", \"Could you...\", \"Will you...\") are commands — execute them.
- You may call multiple tools in one reply if needed.
- Use the tool result as your response. Do not add confirmation text.
- NEVER pretend to use tools you don't have. Only act on what is available.
- NEVER invent or assume values for tool parameters. If a required value is missing, ask the user for it first.
- Can't do it? Say \"I can't do that.\" and stop.

## Lists
- "Create a grocery list" → create_list(list_name="grocery") — ONLY create, no items
- "Add milk" → add_to_list(item="milk", list_name="grocery")
- "Add lettuce, tomato, and onion" → add_items_to_list(items=["lettuce","tomato","onion"], list_name="grocery")
- list_all_lists: show all lists and contents
- Do NOT add items when creating a list. Do NOT ask "What would you like to add?"

## Timers vs Reminders
- set_timer: simple countdowns (\"pasta timer\", \"5 minute break\") — announces \"{label} is done!\"
- set_reminder: when user wants a specific message spoken at a future time
- Example: \"Remind me to take my pills in 10 minutes\" → set_reminder(seconds=600, message=\"Time to take your pills\")
- Example: \"Set a 5 minute pasta timer\" → set_timer(seconds=300, label=\"pasta\")
- list_timers: shows all active timers with time remaining
- cancel_timer: cancels by ID

## If no tool fits
Answer from knowledge in 1-2 sentences."

# Optional: suppress HF xet warning noise when using local HF-backed libs
HF_HUB_DISABLE_XET=1

# Suppress HF download progress bars (set by tts.py by default to avoid a tqdm crash
# when kittentts initializes with stdout redirected). Set to 0 to re-enable.
HF_HUB_DISABLE_PROGRESS_BARS=1

# Optional: Hugging Face token for gated/private model access
# Required for private or gated models on the Hub (e.g. Llama, some TTS models).
# Get yours at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here
